
# Built APKs. A 7 MB binary belongs beside a release, not in the history.
*.apk

# Written by deploy.sh when a deploy finishes, so the next one knows what is
# actually serving. Per-server, never committed.
.deployed-revision
