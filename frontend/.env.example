# The public half of the Cloudflare Turnstile widget. Must match
# TURNSTILE_SITE_KEY in the backend .env. Left empty, the widget is never
# rendered and its script is never fetched.
VITE_TURNSTILE_SITE_KEY=
